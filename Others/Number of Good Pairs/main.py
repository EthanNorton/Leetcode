# a bit harder than it seems, coming back

class Solution:
    def numIdenticalPairs(self, nums: List[int]) -> int:
          for i in nums:
            if nums[i] == nums[j] and i < j:
                

        return good 
        
        # WORKING